# Runtime dependencies of the collector daemon (arb.shell.collect):
websockets>=12    # both venues' orderbook feeds
cryptography>=42  # Kalshi websocket request signing (RSA-PSS)

# The decision core itself (arb/) is standard-library only.
# Development tooling:
pytest>=8
mypy>=1.14
